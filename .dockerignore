# Dependencies – installed fresh in the builder stage via npm ci
node_modules/

# Next.js build output – rebuilt inside the container
.next/
out/

# Git history
.git/

# Environment files – never bake secrets into the image
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db

# TypeScript incremental cache
tsconfig.tsbuildinfo

# Local uploads (served via Docker volume in production)
public/uploads/
